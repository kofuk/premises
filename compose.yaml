x-common-settings: &settings
  PREMISES_CONOHA_USERNAME:
  PREMISES_CONOHA_PASSWORD:
  PREMISES_CONOHA_TENANT_ID:
  PREMISES_CONOHA_IDENTITY_SERVICE:
  PREMISES_CONOHA_COMPUTE_SERVICE:
  PREMISES_CONOHA_VOLUME_SERVICE:
  PREMISES_CONOHA_IMAGE_SERVICE:
  PREMISES_CONOHA_NAME_TAG:
  PREMISES_S3_BUCKET:
  PREMISES_S3_FORCE_PATH_STYLE:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  AWS_REGION:
  AWS_ENDPOINT_URL:
  PREMISES_GAME_OPERATORS:
  PREMISES_GAME_WHITELIST:
  PREMISES_SECRET:
  PREMISES_ALLOWED_ORIGIN:
  PREMISES_REDIS_ADDRESS: redis:6379
  PREMISES_REDIS_USER: ''
  PREMISES_REDIS_PASSWORD: ''
  PREMISES_POSTGRES_HOST: postgres
  PREMISES_POSTGRES_PORT: 5432
  PREMISES_POSTGRES_USER: premises
  PREMISES_POSTGRES_DB: premises
  PREMISES_GAME_DOMAIN:
  PREMISES_ICON_URL:
  PREMISES_PROXY_BACKEND_ADDRESS:

services:
  nginx:
    image: nginxinc/nginx-unprivileged:1.29.5-alpine-slim@sha256:4011c42f28e9b54c86b52211598dbc6bcaa520311ddd55f211587cdd71f88a9c
    restart: always
    volumes:
      - type: bind
        source: ./etc/nginx.conf
        target: /etc/nginx/conf.d/default.conf
        read_only: true
    ports:
      - target: 8000
        published: 8000
        protocol: tcp
        mode: host

  web:
    image: ghcr.io/kofuk/premises:0.12.0
    environment:
      <<: *settings
      PREMISES_MODE: web
      PREMISES_SERVE_STATIC: 'true'
    read_only: true
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    restart: always

  proxy:
    image: ghcr.io/kofuk/premises:0.12.0
    environment:
      <<: *settings
      PREMISES_MODE: proxy
    read_only: true
    ports:
      - target: 25530
        published: 25530
        protocol: tcp
        mode: host
      - target: 25565
        published: 25565
        protocol: tcp
        mode: host

  cron:
    image: ghcr.io/kofuk/premises:0.12.0
    environment:
      <<: *settings
      PREMISES_MODE: cron
    read_only: true

  redis:
    image: valkey/valkey:9.0.3@sha256:3eeb09785cd61ec8e3be35f8804c8892080f3ca21934d628abc24ee4ed1698f6
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 1s
      timeout: 30s
      retries: 30
    restart: always
    volumes:
      - type: volume
        source: redis
        target: /data

  postgres:
    image: postgres:18.3@sha256:98f32d2e093deea07127bcc373f89729b77ff3486511cc77224c530e63a41f0e
    environment:
      POSTGRES_USER: premises
      POSTGRES_DB: premises
      POSTGRES_HOST_AUTH_METHOD: trust
    restart: always
    healthcheck:
      test: ["CMD", "pg_isready", "-Upremises"]
      interval: 1s
      timeout: 30s
      retries: 30
    volumes:
      - type: volume
        source: db
        target: /var/lib/postgresql

volumes:
  db:
  redis:

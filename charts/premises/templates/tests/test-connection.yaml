apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "premises.fullname" . }}-test-connection"
  labels:
    {{- include "premises.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "premises.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

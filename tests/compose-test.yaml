services:
  web:
    environment:
      - 'PREMISES_MC_MANIFEST_URL=https://storage.googleapis.com/premises/devtools/mcserver-fake/version_manifest.json'
      - 'premises_controlPanel_proxyApi=http://host.docker.internal:8001'
      - 'premises_controlPanel_gameDomain=localhost'
    extra_hosts:
      - host.docker.internal:host-gateway
  proxy:
    profiles:
      - disable
